#include "monty.h"
/**
 * f_stack - outputs the top
 * @head:address of stack head
 * @counter: line_number counter
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
